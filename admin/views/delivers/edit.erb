<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:delivers, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:delivers, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:delivers, :edit, :id => @deliver.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :deliver, url(:delivers, :update, :id => @deliver.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'delivers/form', :locals => { :f => f } %>
  <% end %>
</div>
