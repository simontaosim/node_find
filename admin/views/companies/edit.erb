<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:companies, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:companies, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:companies, :edit, :id => @company.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :company, url(:companies, :update, :id => @company.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'companies/form', :locals => { :f => f } %>
  <% end %>
</div>
